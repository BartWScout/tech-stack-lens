# Epsilon

Test fixture — orphan file in a sibling top-level directory. No frontmatter by
design. Validates that (a) the scanner still discovers it, and (b) the scanner
classifies it as `missing-date`.
